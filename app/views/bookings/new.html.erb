<h1>Create a booking for <%= @ship.name %></h1>

<div class="form_box justify-content-center">
  <%= simple_form_for [@ship, @booking] do |book| %>
    <%= book.input :start_date%>
    <%= book.input :end_date %>
    <%= book.submit %>
  <% end %>
</div>
