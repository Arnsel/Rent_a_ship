<h1>Create a new ship listing</h1>
<%= simple_form_for @ship do |ship| %>
  <%= ship.input :name%>
  <%= ship.input :size %>
  <%= ship.input :price %>
  <%= f.input :picture, as: :file %>
  <%= ship.submit %>
<% end %>
