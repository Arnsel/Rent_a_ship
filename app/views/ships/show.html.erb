<h1>Selected Ship</h1>

<h2><%= @ship.name %></h2>
<h1><%= @ship.size %></h1>
<h2><%= @ship.price %></h2>
<%= link_to "Book", new_ship_booking_path(@ship)%>
<%= link_to "View your bookings", ship_bookings_path(@ship)%>
<%= link_to "Edit", edit_ship_path(@ship)%>
<%= link_to "Back", root_path %>
<%= link_to "Delete", ship_path, data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
